Full Stack Search App

A React frontend with TypeScript and Axios fetches data from an ASP.NET Core Minimal API backend. 

The API uses Swagger, AutoMapper, and In-Memory EF Core. The app features a search bar to filter person cards by name using debouncing.
